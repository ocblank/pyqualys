# -*- coding: utf-8 -*-
import logging
from .utils.connect import QualysAPI

logging.basicConfig(level=logging.INFO,
                    format="%(asctime)s \
                    - %(name)s - \
                    %(levelname)s - \
                    %(message)s")
